doctype html
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        title=title
    body 
        canvas#canvas 
        script=`var DISCORD_CLIENT_PUBLIC_ID = '${DISCORD_CLIENT_PUBLIC_ID}';`
        script.
            var Module = {
                canvas : (() => document.getElementById("canvas"))()
            }
        script(type="module", src="/bundle.js")